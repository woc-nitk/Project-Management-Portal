import React from "react";
import Modal from "react-modal";

export default function OrganizationProjects() {
  return (
    <div>
      {/* 
      Get the org id from params,
      Show org name
      Give add org admin button
      Display the admin project component
      */}
    </div>
  );
}
